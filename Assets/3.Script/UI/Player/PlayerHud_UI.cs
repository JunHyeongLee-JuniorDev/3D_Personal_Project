using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHud_UI : MonoBehaviour
{
    [SerializeField] private FakeSlider_UI playerHp;
    [SerializeField] private FakeSlider_UI playerSp;
    [SerializeField] private FakeSlider_UI playerAp;
}
